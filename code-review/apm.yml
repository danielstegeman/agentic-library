name: code-review
version: 2.0.0
description: Two-axis code review skill. Reviews a diff since a fixed point (commit, branch, tag, PR, or merge-base) along independent Standards and Spec axes, run as parallel subagents and reported side by side without reranking.
author: danielstegeman

dependencies:
  apm: []
  mcp: []
