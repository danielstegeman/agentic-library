# Example run config for the _ATF pipeline.
# Generated by New-PipelineRunConfig.ps1; user-editable before invoking.
# Pipeline parameters that aren't listed here will fall back to the pipeline's defaults.

pipeline: _ATF
branch: feature/my-branch

parameters:
  # type: string
  ReleaseDir: 'C:\Releases\2023.1'

  # type: string
  Environment: T01

  # type: string
  ATFServer: ATF01

  # type: string
  ATFDeploymentMode: OverwriteExisting

  # type: string
  RobotParams: ''

variables: {}

# Optional list of stage names to skip:
stagesToSkip: []
